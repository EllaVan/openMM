{
  "description": "数据集特征提取配置示例",
  "mosei": {
    "base_dir": "/path/to/MOSEI",
    "label_file": "/path/to/MOSEI/label.csv",
    "output_dir": "./output/mosei",
    "feature_config": "config_roberta_hubert_vit.json",
    "notes": {
      "base_dir_structure": "应包含 audio/, video/, text/ 目录",
      "label_file_columns": "必须包含 video_id, clip_id, text, emotion/voted_emotion 列",
      "feature_config": "可选，使用 null 则为默认配置"
    }
  },
  "meld": {
    "base_dir": "/path/to/MELD",
    "output_dir": "./output/meld",
    "split": "all",
    "feature_config": "config_roberta_hubert_vit.json",
    "notes": {
      "base_dir_structure": "应包含 train/, dev/, test/ 目录",
      "split_options": "train, dev, test, all",
      "each_split_has": "audio/, video/, label/merged_label_new.csv"
    }
  }
}
