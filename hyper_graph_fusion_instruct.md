



我来详细解释这篇论文中的超图构建和传播机制。

## 一、超图构建过程

### 1. **超图初始化（Hypergraph Initialization）**

该方法使用基于相关性的超图构建方法来初始化超图结构，以确保构建的超图的有效性。

**核心思想：** 将三个模态（文本、音频、视觉）的所有顺序时间步作为节点集合来构造超图结构，旨在探索跨单模态、双模态和三模态的多维情感关系。

**具体步骤：**

- **节点表示**：将节点特征 N^(l)_m ∈ ℝ^(T_m×d) 简化为 N = [n₁; n₂; ⋯; n_Tm]，其中 nᵢ 表示每个模态的第 i 个时间步
- **超图定义**：超图记为 G = (N, E, W)，其中 W 设置为单位矩阵
- **连接矩阵构建**：

```
  Ĥ = (W_N · N)(W_E · N)^T     (公式2)
  H = softmax(Ĥ / √d)          (公式3)
```

其中 d 是单模态编码器输出特征的维度

- **矩阵特性**：H 连续取值且落在 [0,1] 之间

### 2. **超图增强（Hypergraph Augmentation）**

初始超图构建后，其节点和超边的连接关系可能存在冗余，导致后续特征融合时相互干扰，从而影响情感预测。

**解决方案：**

设计超图增强模块，通过扰动超边集合来调整模态高阶交互

**具体操作：**

- 基于初始化的超图，采用均匀分布随机删除现有超边
- 底层假设是初始构建超图时超边数量充足，部分缺失的高阶关系不会对超图语义产生显著影响
- 为保持连续性和完整性，引入监督和非监督对比学习约束

## 二、超图传播过程（超图卷积）

### 1. **传播机制**

超图卷积的作用是根据超图关系融合单模态、双模态和三模态不同维度的信息，用于建模多模态之间的高阶情感关系。

**两阶段聚合过程：**

**阶段1：节点到超边聚合** 首先，由同一超边连接的、表达相同情感的特征被聚合，此时聚合的特征（超边特征）携带属于同一超边的节点的共同情感属性

**阶段2：超边到节点聚合**  
然后通过超边卷积，共同特征被传递给同一超边的节点。经过此过程，每个节点特征被更新并携带其关联节点的高阶情感特征

### 2. **数学公式**

**卷积操作定义：**

```
G^(l)_m = D^(-1/2)_(n,m) H^(l)_m W^(l)_m D^(-1/2)_(e,m) H^T(l)_m D^(-1/2)_(n,m)  (公式4)

N^(l+1)_m = σ(G^(l)_m N^(l)_m θ^(l)_m)  (公式5)
```

**参数说明：**

- H_m：节点连接关系的邻接矩阵
- l：超图卷积层数
- θ_m：训练中学习的参数
- D_(n,m) 和 D_(e,m)：节点和超边的度矩阵
- σ：激活函数
- W^(l)_m：权重矩阵

### 3. **核心创新**

与之前的方法不同，该方法提出多模态超图构建，同时为文本、音频和视觉三个模态构建超图，以实现模态内部和跨模态的高阶信息挖掘。

论文提出的多模态超图网络模型能够同时构建来自三个模态不同时间步的多模态超图，能够建模模态本身的高阶特征及其相互关系。

## 三、完整流程总结

1. **编码器** → 获得三模态的时序节点特征
2. **超图初始化** → 基于相关性构建初始连接矩阵
3. **超图增强** → 随机删除部分超边以减少冗余
4. **超图卷积** → 多层传播聚合高阶情感特征
5. **对比学习** → 优化超图结构和特征表示
6. **情感预测** → 融合节点和超边特征输出结果

这种设计使模型能够自适应地同时捕获单模态、双模态和三模态时间序列节点之间的关系。

# 超图初始化详细说明

我来详细解释超图初始化的具体操作步骤。

## 一、输入准备

### 1. **节点特征获取**

首先，通过单模态编码器（Bi-LSTM）处理三个模态的输入：

```
S_m = FC(BiLSTM(U_m; θ^lstm_m)), m ∈ {t, a, v}
```

**得到的节点特征：**

- 文本节点：N_t ∈ ℝ^(T×d)
- 音频节点：N_a ∈ ℝ^(T×d)
- 视觉节点：N_v ∈ ℝ^(T×d)

其中：

- T 是时间步数量（三个模态对齐后相同）
- d 是统一的特征维度（通过全连接层映射到相同维度）

**关键点：** 将所有三个模态的时间步节点放在一起，共同构建一个多模态超图！

### 2. **节点集合定义**

将三模态的所有时间步作为节点集合：

```
N = [n_1; n_2; ...; n_T; n_(T+1); ...; n_(2T); n_(2T+1); ...; n_(3T)]
    └─ 文本节点 ─┘  └── 音频节点 ──┘  └──── 视觉节点 ────┘
```

**总节点数：** 3T 个节点（每个模态 T 个时间步）

## 二、超图初始化的核心步骤

### 1. **构建连接矩阵的中间表示**

论文使用了基于相关性的方法来计算节点之间的关联性：

```
Ĥ = (W_N · N)(W_E · N)^T
```

**详细解释：**

**Step 1：节点嵌入**

```
Z_N = W_N · N ∈ ℝ^(3T×d')
```

- W_N 是可学习的权重矩阵，用于将节点特征投影到新的空间
- 得到节点的嵌入表示

**Step 2：超边嵌入**

```
Z_E = W_E · N ∈ ℝ^(K×d')
```

- W_E 是可学习的权重矩阵
- K 是超边的数量（可以设定）
- 得到潜在超边的嵌入表示

**Step 3：计算相似度矩阵**

```
Ĥ = Z_N · Z_E^T ∈ ℝ^(3T×K)
```

- 计算每个节点与每个超边之间的相关性分数
- Ĥ[i,j] 表示第 i 个节点与第 j 条超边的关联强度

### 2. **归一化得到关联矩阵**

```
H = softmax(Ĥ / √d)
```

**作用：**

1. **温度缩放：** 除以 √d 防止数值过大，稳定训练
2. **Softmax 归一化：** 对每个节点，在所有超边上做 softmax

```
   H[i,j] = exp(Ĥ[i,j]/√d) / Σ_k exp(Ĥ[i,k]/√d)
```

3. **结果特性：** H 中每个元素在 [0,1] 之间，每行和为 1

### 3. **关联矩阵的含义**

**H ∈ ℝ^(3T×K) 的物理意义：**

```
        超边1  超边2  超边3  ...  超边K
节点1  [ 0.3   0.1   0.5   ...  0.1 ]
节点2  [ 0.2   0.6   0.1   ...  0.1 ]
...
节点3T [ 0.4   0.2   0.3   ...  0.1 ]
```

- **行：** 代表节点（共 3T 个，包含所有模态的所有时间步）
- **列：** 代表超边（共 K 条）
- **H[i,j]：** 节点 i 属于超边 j 的程度（权重）

## 三、为什么这样设计？

### 1. **多模态统一建模**

传统方法的问题：

```
文本超图 → 只连接文本节点
音频超图 → 只连接音频节点  
视觉超图 → 只连接视觉节点
❌ 无法建模跨模态的高阶关系
```

本文的方案：

```
多模态超图 → 一个超边可以同时连接：
            - 文本的某些时间步
            - 音频的某些时间步
            - 视觉的某些时间步
✓ 可以建模单模态、双模态、三模态的高阶关系
```

### 2. **基于相关性的柔性连接**

**传统图的问题：**

```
节点A ──边── 节点B  (硬连接，0或1)
```

**超图的优势：**

```
        ┌─ 节点A (权重 0.3)
超边1 ──┼─ 节点B (权重 0.6)
        └─ 节点C (权重 0.1)
```

**相关性方法的优势：**

- 通过学习 W_N 和 W_E，自动发现哪些节点应该连接
- 连接强度是连续值，更灵活
- 可以捕捉复杂的情感关系模式

### 4. **图对比学习**

为了增强模型的表征能力和判别性，我们采用图对比学习。对比学习的目标是通过最大化正例之间的相似度，并最小化负例之间的相似度，从而让同一实例的不同模态表示更加一致，不同实例的表示则更加区分。

#### **对比学习损失**

- **正例**：同一实例的不同模态视为正例。
    
- **负例**：不同实例的不同模态视为负例。
    

对比学习损失采用 **supervised contrastive loss**，其中：

```
Lcontrastive=−1∣P(i)∣∑p∈P(i)log⁡exp⁡(zi⋅zp/τ)∑a∈A(i)exp⁡(zi⋅za/τ)\mathcal{L}_{\text{contrastive}} = -\frac{1}{|P(i)|} \sum_{p \in P(i)} \log \frac{\exp(z_i \cdot z_p / \tau)}{\sum_{a \in A(i)} \exp(z_i \cdot z_a / \tau)}Lcontrastive​=−∣P(i)∣1​p∈P(i)∑​log∑a∈A(i)​exp(zi​⋅za​/τ)exp(zi​⋅zp​/τ)​
```

其中，P(i)P(i)P(i) 是同标签的正例集合，A(i)A(i)A(i) 是所有候选对。

---

### 5. **权重正则化与Bottleneck 层**

#### **权重正则化**

- 对超边权重 wew_ewe​ 和投影层的权重 WprojW_{\text{proj}}Wproj​ 施加 **L2 正则化**，防止过拟合。
    ```
    权重正则化项如下：
    
    Lreg=λ∑e∥we∥22+λproj∑i∥Wi∥22\mathcal{L}_{\text{reg}} = \lambda \sum_e \| w_e \|_2^2 + \lambda_{\text{proj}} \sum_i \| W_i \|_2^2Lreg​=λe∑​∥we​∥22​+λproj​i∑​∥Wi​∥22​
    ```
    
#### **Bottleneck 层**

- 在超图卷积和对比学习的过程中，可以在合适的位置加入 **Bottleneck 层**，将高维的节点表示压缩到较低维度，去除冗余信息。
    
    在超图卷积后的节点表示上加一个瓶颈层：
    
    ```
    zbottleneck=Bottleneck(zconv)z_{\text{bottleneck}} = \text{Bottleneck}(z_{\text{conv}})zbottleneck​=Bottleneck(zconv​)
    ```
    
    在对比学习的投影头前或后加入瓶颈层，进行维度压缩：
    
    ```
    zprojected=Bottleneck(z)z_{\text{projected}} = \text{Bottleneck}(z)zprojected​=Bottleneck(z)
    ```

---

### 6. **训练与优化**

- **优化器**：使用 **AdamW** 优化器来优化所有模型参数，包括超图卷积、投影头、分类器和瓶颈层。
    
- **损失函数**：总损失函数为分类损失、对比损失和正则化项的加权和：
    
    ```
    L=Lclassification+λcLcontrastive+λregLreg\mathcal{L} = \mathcal{L}_{\text{classification}} + \lambda_{\text{c}} \mathcal{L}_{\text{contrastive}} + \lambda_{\text{reg}} \mathcal{L}_{\text{reg}}L=Lclassification​+λc​Lcontrastive​+λreg​Lreg​
    ```
    
    其中：
    
    - **分类损失**：标准的交叉熵损失。
        
    - **对比损失**：通过图对比学习增强表征。
        
    - **正则化损失**：防止权重过大导致的过拟合。
        

---

### 7. **输出：分类器权重**

经过训练后，输出的分类器权重会用于对新样本进行情感分类。在测试阶段，使用训练得到的分类器权重对新样本进行推理。

---

### **技术方案总结**

- **超图构建**：通过将每个样本内的多模态特征通过超边连接，并基于 KNN 构建跨样本的模态连接，形成多模态超图。
    
- **超图卷积与传播**：使用两阶段的超图卷积聚合节点和超边信息，增强模态间的高阶关系。
    
- **图对比学习**：利用对比学习强化同一实例不同模态间的表示一致性，同时保证不同实例的模态之间有区分性。
    
- **权重正则化与Bottleneck层**：在超边权重和投影层上应用L2正则化，并在关键位置引入Bottleneck层，降低模型的复杂度，避免过拟合。
    
- **训练**：使用交叉熵损失、对比学习损失与正则化损失共同优化模型，最终得到分类器权重。
